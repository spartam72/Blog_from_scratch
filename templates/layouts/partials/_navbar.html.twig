<nav class="navbar navbar-expand-lg navbar-light">
	<div class="container">
		<a class="navbar-brand fst-italic" href="{{ path('app_home') }}">Panterest</a>
		<button class="navbar-toggler bg-white" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav ms-lg-auto mb-2 mb-lg-0 text-white">
				<li class="nav-item">
					<a class="nav-link text-white {{ myRoute == 'app_home' ? 'actif' : '' }}" aria-current="page" href="{{ path('app_home') }}">Home</a>
				</li>
				<li class="nav-item">
					<a class="nav-link text-white {{ myRoute == 'app_articles' ? 'actif' : '' }}" href="{{ path('app_articles') }}">Articles</a>
				</li>
				{% if is_granted('IS_AUTHENTICATED_FULLY') %}
					<li class="nav-item d-flex align-items-center my-lg-0 my-3 ms-lg-3">
						<img src="{{ asset('build/img/f_profil_defaut.jpg')}} " width="40" class="rounded-circle">
					</li>
					<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle text-success" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
							{{app.user.fullName}}
						</a>
						<ul class="dropdown-menu dropdown-menu-dark" aria-labelledby="navbarDropdown">
							<li>
								<a class="dropdown-item {{ myRoute == 'app_article_new' ? 'actif' : '' }}" href="{{ path('app_article_new') }}">Créer article</a>
							</li>
							<li>
								<a class="dropdown-item" href="#">Compte</a>
							</li>
							<li><hr class="dropdown-divider"></li>
							<li>
								<a class="dropdown-item text-danger " href="{{ path('app_logout') }}">Logout</a>
							</li>
						</ul>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="nav-link text-primary" href="#">Register</a>
					</li>
					<li class="nav-item">
						<a class="nav-link text-success" href="{{ path('app_login') }}">Login</a>
					</li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
